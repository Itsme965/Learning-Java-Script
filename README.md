# Learning-Java-Script

A simple JavaScript playpen that deploys automatically to GitHub Pages.

## How it works

- Edit `index.html` and `script.js`.
- Push to `main`.
- GitHub Actions deploys the site using `.github/workflows/deploy-pages.yml`.

Your live page URL will be available in your repo under **Settings > Pages** after the first successful deployment.